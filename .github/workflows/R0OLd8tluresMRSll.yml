name: ujMYwnJa9XYJOnajtf

on:
  workflow_dispatch:
  
  schedule:
    # UTC 时间
     - cron: "0,30 * * * *"    
  watch: 
      types: started
      
jobs:
  v2tbdS49t3vH4xoCT:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests
        pip install fake-useragent
        pip install aiohttp
    - name: Run
      run: |
        python ./post/about/c.py
